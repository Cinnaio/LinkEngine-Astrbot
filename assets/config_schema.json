{
  "api_url": {
    "description": "LinkEngine API 地址",
    "type": "string",
    "default": "http://127.0.0.1:8192",
    "hint": "MC 服务器插件的 HTTP API 地址，格式: http://IP:端口"
  },
  "api_key": {
    "description": "API 密钥",
    "type": "string",
    "default": "change-me-to-a-random-key",
    "hint": "与 MC 服务器 config.yml 中 api.api-key 保持一致"
  },
  "admins": {
    "description": "管理员列表",
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": [],
    "hint": "管理员的用户ID列表（如QQ号），可执行管理类命令"
  },
  "player_bindmap": {
    "description": "玩家绑定映射",
    "type": "dict",
    "default": {},
    "hint": "用户ID到MC UUID的映射，用于 /town my 等命令。格式: {\"QQ号\": \"MC-UUID\"}"
  },
  "registration_webhook_path": {
    "description": "EnderPass 注册通知路径",
    "type": "string",
    "default": "/enderpass/registration",
    "hint": "与 EnderPass 配置的 Webhook URL 路径一致，复用 OAuth 回调服务端口"
  },
  "registration_webhook_secret": {
    "description": "EnderPass 注册通知密钥",
    "type": "string",
    "default": "",
    "hint": "与皮肤站 EnderPass 的 astrbot_webhook_secret 保持一致"
  },
  "registration_notify_groups": {
    "description": "注册通知群号",
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": [],
    "hint": "收到新玩家注册后发送通知的群号列表；留空表示只记录不发送"
  },
  "invitation_api_url": {
    "description": "EnderPass 邀请 API 地址",
    "type": "string",
    "default": "",
    "hint": "默认复用 oidc_issuer；只有 EnderPass API 使用独立地址时才需要填写"
  },
  "invitation_api_secret": {
    "description": "EnderPass 邀请 API 密钥",
    "type": "string",
    "default": "",
    "hint": "与 EnderPass 配置中的 enderpass_astrbot_api_secret 保持一致，不要与 webhook 或 MC API Key 复用"
  },
  "invitation_api_timeout": {
    "description": "EnderPass 邀请 API 超时（秒）",
    "type": "int",
    "default": 10,
    "hint": "邀请码查询与生成请求的最大等待时间"
  },
  "invitation_history_limit": {
    "description": "邀请码历史显示条数",
    "type": "int",
    "default": 10,
    "hint": "私聊 /邀请记录 时最多显示的最近记录数"
  },
  "invitation_invitee_limit": {
    "description": "邀请名单显示条数",
    "type": "int",
    "default": 20,
    "hint": "私聊 /邀请名单 时最多显示的最近邀请数"
  }
}
